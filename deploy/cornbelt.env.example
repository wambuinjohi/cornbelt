# Copy this file to /etc/cornbelt.env and set secure values (do NOT commit secrets to git)
NODE_ENV=production
API_BASE_URL=https://cornbelt.co.ke
DB_HOST=127.0.0.1
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=your_db_name
JWT_SECRET=your_jwt_secret
# Optionally: PORT for Node server (default: 3000)
PORT=3000
