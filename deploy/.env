# Copy this file to your webroot as ".env" (e.g. /var/www/cornbelt/.env)
# DO NOT commit real secrets to git. Replace placeholder values with production credentials.

# Database (MySQL/Postgres connection settings used by api.php)
DB_HOST=127.0.0.1
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=your_db_name

# JWT secret used to sign admin tokens (use a long random string)
JWT_SECRET=please_change_this_to_a_secure_random_string

# API base URL used by server side (optional)
API_BASE_URL=https://cornbelt.co.ke

# Optional: port for Node server if you also run the Node app locally
PORT=3000

# Enable detailed API debug responses (set to 0 in production)
API_DEBUG=1
